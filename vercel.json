{
  "version": 2,
  "builds": [
    { "src": "user/server.py", "use": "@vercel/python" },
    { "src": "**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/user/(.*)", "dest": "user/$1" },
    { "src": "/(.*)", "dest": "$1" }
  ]
}
